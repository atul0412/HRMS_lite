# Optional. Set to your backend base URL when deploying or when the frontend is not served with a proxy.
# Example: https://hrms-lite-api.onrender.com
# Leave unset when using Vite dev server with default proxy to http://127.0.0.1:8000
# VITE_API_URL=
